@AGENTS.md

Claude Code adapter notes (everything else is in `AGENTS.md` and `.ai/`):

- `.claude/agents/*.md` only register the canonical roles in `.ai/roles/`;
  the instructions live there.
- `.claude/skills/**` is generated by Laravel Boost (`boost.json`) — never
  hand-edited. Project skills live in `.ai/skills/`.
